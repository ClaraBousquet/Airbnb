{% extends 'base.html.twig' %}

{% block title %}{{ house.name }}{% endblock %}

{% block body %}



            <section class="d-flex gap-5 flex-wrap justify-content-center flex-row details">

                <div class="card">
                    <h2>{{ house.name }}</h2>
                    <h3>{{ house.address }}</h3>

                    <p>{{ house.description }}</p>

                    <img class="img" src="/images/upload/{{ house.imagePath }}" alt="house">
                    <p>{{ house.price }}</p>
                </div>
            </section>

            <button >Réserver</button>
            {% endblock %}