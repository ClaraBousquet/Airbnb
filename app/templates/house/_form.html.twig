<div class="form-container">
{{ form_start(form, {'attr': {'class': 'custom-form'}}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('accueil') }}" class="btn btn-danger  ">Retour</a>

{{ form_end(form) }}
</div>